{
  "name": "the_arcane_arsenal",
  "version": "1.0.0",
  "description": "Tabletop RPG Character App",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "client": "npm run dev --prefix client",
    "server": "npm run app --prefix server",
    "dev": "concurrently \"npm run server\" \"npm run client"
  },
  "author": "C.M.Johnson",
  "license": "MIT",
  "dependencies": {
    "concurrently": "^8.2.2"
  }
}
